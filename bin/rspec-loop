#!/bin/bash

# Used to run flaky js specs multiple times
export BUNDLE_GEMFILE=$PWD/Gemfile

for i in {1..10}; do
  bin/rspec $@ || (echo "SHOW ENGINE INNODB STATUS;" | spec/dummy/rails-*/bin/rails dbconsole -p -e test)
done;

exit 1
